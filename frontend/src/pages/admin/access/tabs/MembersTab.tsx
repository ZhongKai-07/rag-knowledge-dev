/** P1.5b 将在此 Tab 填入组织树 + 成员详情面板。P1.5a 仅保留占位。 */
export function MembersTab() {
  return (
    <div className="rounded-lg border border-dashed bg-white p-8 text-center text-sm text-slate-400">
      团队成员面板将在 P1.5b 接入（组织树 + 成员角色分配 + 因角色可访问的 KB 列表）。
    </div>
  );
}
